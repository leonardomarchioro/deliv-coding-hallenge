import User from "./user.validation";
import Auth from "./auth.validation"
import Request from "./request.validation"
import Address from "./address.validation";

const Validation = {
    User,
    Auth,
    Request,
    Address
}

export default Validation;